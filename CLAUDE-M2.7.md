# Global CLAUDE.md

## 输出要求
- 最终回复必须使用自然、地道的中文，避免翻译腔。
- 结论先行。优先给出代码变更、命令结果、修复结论或可执行方案。
- 保持高信息密度与极简表达；无关背景默认不展开。
- 不确定项直接标注 `[待确认]`，避免模糊表述。

## 检索与修改原则
- 在修改核心逻辑前，必须先检索全局引用、调用链和相关配置，避免局部盲改。
- 检索时优先使用专业英文关键词；若仓库或文档以中文术语为主，需同时使用中文关键词交叉检索。
- 写操作前必须先读取并理解相关上下文；未读完上下文不得直接修改。
- 严格遵守最小修改原则，不改动与当前任务无关的代码。

## 工程执行标准
- 自动对齐项目现有的命名、缩进、文件组织、错误处理和架构风格。
- 优先复用现有实现、工具链和模式，避免无必要的新抽象或新依赖。
- 采用防御性编程，补齐必要的异常处理、边界判断和失败路径。
- 优先写自说明代码；仅对非显然逻辑、边界条件、兼容性分支和公共接口补充必要注释，并同步更新失效注释。

## 验证要求
- 修改后必须执行与改动范围匹配的最小必要验证，优先复用现有测试、lint、type-check 或最小复现脚本。
- 若验证失败，先基于错误日志自主修复；连续两轮失败后，再汇报阻塞点与已尝试路径。
- 汇报结果时，优先给出：改动文件、关键变更、执行命令、验证结果、剩余风险。

## 工具使用
- 优先使用系统工具完成检索、批量修改、比对和验证。
- 涉及跨文件逻辑时，先检索再修改，确保一致性。
