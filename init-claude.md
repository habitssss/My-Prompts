你要为当前代码仓库生成一份“项目级 CLAUDE.md”。

目标不是写通用 AI 助手人格设定，而是写一份给 Claude Code / agent 模型在本仓库中执行编程任务时使用的、短小但高约束的项目操作说明。该文件将与全局 CLAUDE.md 一起使用，因此你必须避免重复全局规则，只保留“本项目特有且高价值”的内容。

请严格遵循以下要求生成：

# 总体目标
生成一份适合编程场景、适合长上下文 agent、适合 MiniMax M2.7 风格的项目级 CLAUDE.md。
要求：
- 短而硬，不写空话，不写人格描述，不写礼貌措辞
- 只写对本项目真正有帮助的规则
- 优先写“可执行约束”，不要写抽象原则
- 优先减少模型走偏、误改、漏验证、乱重构的概率
- 输出内容应可直接保存为仓库根目录下的 CLAUDE.md

# 你在生成前必须做的事
先阅读并理解仓库，再决定内容。至少要识别：
1. 技术栈与语言（如 TypeScript / Python / Go / Java）
2. 包管理与构建方式（pnpm/npm/yarn/pip/poetry/go mod/maven 等）
3. 测试命令、lint 命令、type-check 命令、build 命令
4. 目录结构与模块边界
5. 主要架构模式（MVC、service-repo、hexagonal、monorepo package 分层等）
6. 错误处理、日志、配置管理、环境变量习惯
7. 是否已有测试框架、mock 方案、fixture 方案
8. 哪些目录或文件属于高风险区域，不应轻易改动
9. 仓库中已有的命名风格、注释风格、代码组织风格

如果信息不足，不要编造；缺失项用 `[待确认]` 标记。

# 生成原则
1. 不要重复这些全局类规则：
- 中文输出
- 结论先行
- 高信息密度
- 读后写
- 最小化变更
- 全局检索
- 工具优先
这些默认已存在于全局 CLAUDE.md。

2. 项目级 CLAUDE.md 只保留：
- 本项目命令
- 本项目目录与边界
- 本项目实现约束
- 本项目验证方式
- 本项目禁区或高风险区域
- 本项目常见任务工作流

3. 规则必须具体：
差示例：
- “遵循最佳实践”
- “注意代码质量”
好示例：
- “修改 API handler 后必须运行 `pnpm test --filter api` 与 `pnpm typecheck`”
- “不要跨 package 引用 internal util，统一从 `packages/shared` 暴露入口导入”
- “涉及数据库 schema 的改动，必须同步检查 migration、types、seed 与 integration tests”

4. 优先写“默认动作”：
例如：
- 改动路由前先检查 schema / handler / service / test
- 改动数据库前先检查 migration / model / repo / seed / test fixture
- 改动共享类型前先检查所有下游 package 引用

5. 验证要求要和项目现状匹配：
- 优先复用现有测试命令
- 没有测试框架时，不要强行要求补完整单测体系
- 只要求“最小必要验证”
- 若某类改动有固定验证路径，要明确写出来

6. 对 M2.7 友好：
- 控制长度，避免冗长解释
- 章节清晰
- 每条规则尽量一行一个动作
- 避免重复
- 避免大段背景描述
- 不要写“你是资深工程师/架构师”之类人格提示

# 输出格式要求
请直接输出完整的 CLAUDE.md 内容，不要额外解释，不要加前言，不要写“下面是生成结果”。

必须使用以下结构；若某节确实缺信息，可保留标题并标记 `[待确认]`：

# CLAUDE.md

## Project Overview
- 用 3~6 条写清项目类型、核心模块、主要技术栈
- 只写和执行任务强相关的信息

## Commands
- install:
- dev:
- build:
- test:
- lint:
- typecheck:
- format: （如果有）
- 只填仓库真实可用命令；未知写 `[待确认]`

## Repository Structure
- 写清关键目录用途
- 写清模块边界
- 写清哪些目录可以改、哪些目录谨慎改

## Implementation Rules
- 写本项目特有的实现约束
- 包括导入边界、分层要求、状态管理方式、数据访问约束、接口约束、配置约束等
- 每条都应可直接执行

## Validation
- 写本项目里不同改动对应的最小验证方式
- 优先按“改动类型 -> 必跑命令/检查”组织
- 若现有项目没有完善测试，只写现有可执行验证方式

## High-Risk Areas
- 写明高风险目录、基础设施层、公共类型、数据库、迁移、配置、CI、脚本等
- 明确“何时不要碰”或“改动前必须检查什么”

## Task Workflow
- 给出 3~6 条本项目常见任务的默认执行路径
- 例如：
  - 新增 API
  - 修复 bug
  - 重构 service
  - 修改数据库字段
  - 调整前端组件
- 每条路径要短且可执行

## Notes
- 只写少量高价值补充
- 不要写重复规则
- 不要写空泛建议

# 风格要求
- 用英文标题，内容用中文
- 极简
- 不写套话
- 不写背景科普
- 不写与仓库无关的通用建议
- 最终长度尽量控制在 80~180 行内
